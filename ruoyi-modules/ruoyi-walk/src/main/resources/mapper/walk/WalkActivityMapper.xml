<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.walk.mapper.WalkActivityMapper">

    <select id="selectCurrentActivity" resultType="org.dromara.walk.domain.WalkActivity">
        select * from walk_activity where is_current = 'Y' and del_flag = '0' limit 1
    </select>

    <update id="updateWalkActivityStatus">
        update walk_activity set status = #{status}, update_time = sysdate() where activity_id = #{activityId}
    </update>

</mapper>
