<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.walk.mapper.WalkWinnerMapper">

    <select id="selectWinnerByMember" resultType="org.dromara.walk.domain.WalkWinner">
        select * from walk_winner
        where del_flag = '0'
          and activity_id = #{activityId}
          and member_id = #{memberId}
    </select>

    <update id="updateWalkWinnerAddress">
        update walk_winner
        set receiver = #{receiver},
            receiver_phone = #{receiverPhone},
            address = #{address},
            update_time = sysdate()
        where winner_id = #{winnerId}
    </update>

</mapper>
